const pending = require('./pending');
const signups = require('./signups');
const agents = require('./agents');
const imageResizing = require('./image-resizing');

module.exports = { pending, signups, agents, imageResizing };
